public enum Category {
    // we do the class enum so the user can just chosse from the avalible specefic options 
    FOOD,
    TRANSPORTATION,
    SHOPPING,
    HEALTH,
    ENTERTAMENT,
    OTHER
    
}
